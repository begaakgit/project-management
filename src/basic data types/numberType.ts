//All numbers in typescript are floating point value. These floating point numbers get the type Number.

class numberType{
    addValues(a,b){
        console.log (a+b);
    }
}
export default numberType;